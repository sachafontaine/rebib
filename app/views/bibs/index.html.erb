<div class="container">
  <div class="row w-100-custom">
    <div class="program-list mb-4 col-md-12">
      <h2 class="text-center mt-4 mb-4">Dossards en vente sur Rebib</h2>
        <% if @bibs.where(available: "free").count > 0 %>
          <div class="row">
            <% @bibs.where(available: "free").each do |bib| %>
              <div class="col-sm-3 col-md-3 mb-3">
                <%= link_to bib, class:"card-offer" do %>
                  <div class="card-offer">
                    <div class="card-offer-infos">
                      <h4><%= bib.race.name %></h4>
                      <p>📍 <%= bib.race.address.gsub(/([()])/,' \1 ') %></p>
                      <p>🙋🏼‍♂️ Dossard de <%= bib.user.first_name %></p>
                      <p><strong><%= humanized_money_with_symbol(bib.price) %></strong></p>
                    </div>
                  </div>
                <% end %>
              </div>
            <% end %>
          </div>
        <% else %>
        <p>Il n'y a plus de dossard en vente actuellement</p>
          <%= link_to "Vendez votre dossard", new_race_bib_path(Race.all), class:"btn btn-primary" %>
        <% end %>
    </div>
  </div>
</div>
