<div class="container">
  <div class="row mt-4 mb-4 d-flex justify-content-center text-center">
    <div class="col-xs-12 col-sm-8">
      <div class="mt-4">
        <span class="etiquette px-4 py-2">Dossard</span>
      </div>
      <h2 class="mt-3 mb-3"><%= @bib.race.name %></h2>
      <div class="mt-4">
        <% unless @bib.race.date.match?(/DATE NON COMMUNIQUEE/)  %>
          <p>📅 <%= @bib.race.date.gsub(/(au)/, ' au') %></p>
        <% else %>
          <p>📅 La date de cette course n'est pas connue de nos services. <%= link_to "Voir les détails de la course", race_path(@bib.race) %></p>
        <% end %>
      </div>
      <p>📍 <%= @bib.race.address.gsub(/([()])/,' \1 ') %></p>
      <p>🙋🏼‍♂️ Propriétaire de ce dossard : <%= @bib.user.first_name %></p>
      <p>⏰ Déposé le : <%= @bib.created_at %></p>
      <p>💶 Prix : <%= @bib.price %>€</p>
      <hr>
    </div>
  </div>
</div>
